# Explorer Tab Utility 修复版

基于 w4po 原版修改，添加中文界面和主题切换，修复了几个导致无法使用的 Bug。

## 对比原版做了什么

**加了中文界面**
全部菜单、设置、提示翻译成了中文，偏好设置里可以切换回英文。原版只有英文。

**加了深色/浅色主题**
原版只有一个深色主题。现在支持深色、浅色、跟随系统，点一下就能切换。

**修了启动就崩溃的问题**
原版的配色文件里有几处写法在 WPF 里不合法，一点开就报错退出。改正确之后就能正常启动了。

**修了点偏好设置就闪退的问题**
和上面类似，配色文件里另一处不合法的写法，打开偏好设置页就闪退。也修了。

**修了配置会自己变回去的问题**
控件初始化的顺序不对，结果快捷键的"操作类型"会悄悄被改回默认的"打开"。比如你设的是 Alt+M 显示/隐藏，下次打开可能就变成 Alt+M 打开新窗口了。加了个初始化标志解决了。

**修了几个还是英文的弹窗**
重复启动提示、恢复窗口确认、清除历史确认这几个弹窗原来是硬编码的英文，改成了走翻译系统，中文/英文跟着语言设置走。

**配置文件不容易坏了**
保存机制改成了先写临时文件、再备份旧文件、再替换的方式。万一保存过程中断电了，有备份可以恢复。

## 还改了些什么

- 移除了自动更新（通过 GitHub Releases 手动更新）
- 只支持 .NET 9（不再支持 .NET Framework 4.8.1）
- COM 组件改用手动定义，不依赖系统类型库
- 微软雅黑字体优化中文显示
